---
- hosts: localhost
  gather_facts: false
# this is a 'play' include
- import_playbook: listofplays.yml

- name: another play.
  hosts: all
  gather_facts: false
  tasks:
  # this is a 'task' include
    - import_tasks: stuff.yml
    
    - debug: 
        msg: "*** main_import "

